:load Logger

--# firstExample
execLogger (globToRegex "foo*.[ch]")

--# bind.type
:type (>>=)
:type (globToRegex "" >>=)

--# simple
let simple = return True :: Logger Bool
execLogger simple

--# logged
execLogger (record "hi mom!" >> return 3.1337)

--# m
let m = return "foo" :: Logger String

--# m.length
length m

--# m.length2
m >>= \s -> return (length s)
